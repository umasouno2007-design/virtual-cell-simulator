# e-cell 实验数据协作模板

## 适用范围

本模板仅用于收集可与 e-cell 当前贴壁细胞经验动力学模型对齐的结构化记录，帮助后续参数校准和独立留出验证。它**不是**湿实验操作 SOP、伦理文件、生物安全指南或导师指导的替代物；具体培养、采样、废弃物和安全要求必须遵循所属实验室制度。

## 最小元数据

每个实验批次至少记录：细胞系名称与 RRID、来源/供应商、传代数、细胞库或冻存管编号、培养基与血清批次、培养器皿规格、工作体积（mL）、接种密度（cells/cm²）、温度（°C）、CO₂（%）、实验批次 ID 与操作者匿名编号。建议另附 STR 与支原体检测状态、日期和原始报告的受控存放位置。

## 最小时间序列

| CSV 列名 | 含义 | 单位 | 必填 |
| --- | --- | --- | --- |
| `time_h` | 距本批次起点的时间 | h | 是 |
| `viable_cells` | 活细胞数 | cells | 是 |
| `viability_percent` 或 `total_cells` | 存活率或总细胞数 | % / cells | 建议 |
| `glucose_mM` | 培养液葡萄糖 | mM | 是 |
| `lactate_mM` | 培养液乳酸 | mM | 是 |
| `pH` | pH | 无量纲 | 是 |
| `oxygen_percent`、`osmolality_mOsm_kg`、`drug_uM`、周期读出 | 可选环境/读出 | 见列名 | 否 |

`experiment_data.py` 会识别常见中英文别名；最稳妥的做法仍是使用上述标准列名和单位。缺失值留空，不要填 `0`；异常或仪器失败值应在单独的 `quality_flag`、`note` 列标记，避免作为正常测量参与校准。

## 时间点与重复原则

覆盖接种后、指数增长期与可能的营养/代谢压力阶段；记录实际采样时间而非计划时间。使用多个生物学重复估计批次差异，并保留至少一个完整批次或后段时间点作为**独立验证集**。拟合数据与验证数据必须在开始校准前明确分开；不要用同一批次的全部时间点同时拟合和宣称验证。

## 数据边界

公开仓库不得提交患者或供体可识别信息、个人联系方式、未授权实验室数据、受限材料信息、账号凭据或原始仪器日志。真实协作数据应保留可追溯来源、访问权限和伦理/机构审批记录，并只在获授权环境中共享。
