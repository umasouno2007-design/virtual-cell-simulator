# e-cell 模型卡（v1.0.0）

## 用途与非用途

e-cell 是**面向贴壁细胞培养条件探索的、文献驱动的经验动力学研究原型**。它适合教学、实验设计讨论、输入条件敏感性探索，以及与带来源的培养时间序列进行透明的初值探索。

它不用于临床决策、患者风险评估、诊断、治疗预测、GxP 审计、真实细胞器定量测量，或替代湿实验和独立验证。

## 适用范围、输入与输出

- 当前预设：A549、HeLa、HEK-293 的贴壁培养情景；预设不等于对任何批次、传代数或实验室条件的验证。
- 输入：初始活细胞数（cells）、培养体积（mL）、面积（cm²）、葡萄糖/谷氨酰胺/乳酸（mM）、氧/CO₂（%）、pH、温度（°C）、渗透压（mOsm/kg）、药物（µM）与模拟时长（h）。
- 输出：活/死细胞数（cells）、存活率（%）、汇合度（%）、环境浓度/状态趋势；细胞器模块输出的是相对功能指数（%），不是物理定量测量。

## 简化、证据与验证状态

群体增殖以细胞系倍增时间先验换算最大生长率，并叠加营养、氧、pH、温度、渗透压、乳酸、汇合度与药物的经验修正。物料衡算采用简化的摄取/生成与氧传递项。直接关系、待校准先验、演示规则分别标为 A/B/C，来源在 README 与应用“参数来源与可追溯性”中列出。

`growth_scale` 和 `uptake_scale` 只用透明网格搜索作参数初值探索；结果不构成统计推断、置信区间或已验证预测。仓库内 A549 数据是教学合成数据，仅证明评估流程可复跑，**尚未完成独立实验验证**。

## 测试、失败模式与禁止用途

测试覆盖核心数值边界、CSV 标准化/残差、透明校准、教学验证、敏感性分析、场景导入导出和数据质量规则。`python scripts/smoke_check.py` 用于部署前最低自检。

已知失败模式包括：起点不一致、单位错误、时间点不足、参数最佳点落在搜索边界、未建模的批次/传代/污染影响，以及把教学相对指数误解为生物测量值。出现这些情况时不得据此做定量实验、临床或监管结论。

## 版本原则

- patch：不改变模型结果含义的文档、修复或呈现修正；
- minor：新增向后兼容的输出、场景字段或可选分析；
- major：改变核心方程、默认参数含义、单位、输出解释或无法读取旧场景时。
